// Placeholder — the real landing + memory wall lands in W2 (docs/15 §1).
export default function Home() {
  return (
    <main className="flex flex-1 flex-col items-center justify-center gap-4 p-8">
      <h1 className="font-display text-4xl lowercase tracking-tight">one tribe</h1>
      <p className="text-muted">the moments we took home</p>
    </main>
  )
}
